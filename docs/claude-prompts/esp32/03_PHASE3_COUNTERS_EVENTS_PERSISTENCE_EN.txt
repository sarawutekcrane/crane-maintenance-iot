CLAUDE CODE IMPLEMENTATION PROMPT
ESP32 CRANE / HEAVY-EQUIPMENT IoT PLATFORM
PHASE 3 - VEHICLE STATE, COUNTERS, EVENTS, AND PERSISTENCE
Revision 02

======================================================================
PROJECT CONTEXT
======================================================================

Act as a senior embedded systems engineer, IoT firmware architect, backend integration engineer, and heavy-equipment electronics engineer.

This project is an ESP32-based IoT platform for mobile cranes and related heavy equipment. The final system must monitor and manage:

- ignition state,
- engine-running state,
- engine operating hours,
- PTO state,
- PTO operating hours,
- vehicle speed pulse,
- calculated vehicle speed,
- calculated travel distance / odometer,
- engine RPM pulse,
- analog sensor values,
- battery/system voltage,
- fuel level when supported,
- temperature when supported,
- hydraulic pressure when supported,
- GPS/GNSS position,
- network state,
- device health,
- firmware version,
- configuration version,
- offline queued data,
- structured device events,
- remote command status,
- future model-specific emergency output.

CORE RULE:

MEASUREMENT, COUNTER ACCUMULATION, AND LOCAL EVENT RECORDING MUST NEVER DEPEND ON INTERNET AVAILABILITY.

Prototype system:

ESP32 Device
    ->
Backend / API
    ->
Google Sheets Data Service
    ->
Thai-language Web Application

Production system:

ESP32 Device
    ->
Backend / Device Transport Layer
    ->
PostgreSQL
    ->
Thai-language Web Application

Important architecture rules:

1. The browser must NEVER communicate directly with the ESP32.
2. The ESP32 must NEVER write directly to Google Sheets.
3. The ESP32 communicates only with the backend/device transport layer.
4. Google Sheets is a prototype database only.
5. PostgreSQL will become the production source of truth.
6. ESP32 communication must not depend on the backend database technology.
7. All user-facing Web pages must be in Thai.
8. Backend/API/database/device field names and stable technical codes may remain in English.
9. The backend owns fleet/business logic, PM rules, lifetime rules, alerts, permissions, and workflow.
10. The ESP32 owns reliable local measurement, counters, local event generation, offline buffering, and device communication.

======================================================================
CURRENT PROTOTYPE HARDWARE
======================================================================

Controller:
ideaspark ESP32 Development Board
ESP32 classic
Integrated 1.14-inch TFT LCD

Framework:
Arduino Framework

The project should remain suitable for:
- PlatformIO
- Arduino IDE

Future production target:
ESP32-S3 production hardware, exact final module TBD.

Current prototype pin plan:

TFT:
MOSI       GPIO23
SCLK       GPIO18
RESET      GPIO4
DC         GPIO2
CS         GPIO15
BACKLIGHT  GPIO32

Digital inputs:
IGN                GPIO34
PTO                GPIO35
ENGINE_RUN         GPIO36
AUX_DIGITAL_1      GPIO39
SPARE_DIGITAL_1    GPIO13
SPARE_DIGITAL_2    GPIO14

Pulse inputs:
VEHICLE_SPEED      GPIO25
ENGINE_RPM         GPIO26
SPARE_PULSE_1      GPIO27
SPARE_PULSE_2      GPIO33

I2C:
SDA                GPIO21
SCL                GPIO22

Prototype external ADC:
ADS1115 x 2

Potential test outputs:
GPIO16
GPIO17
GPIO19

IMPORTANT:
Do not permanently assign the future emergency output yet.

Use:
EMERGENCY_OUTPUT_PIN = TBD

Keep physical emergency output disabled until the dedicated emergency-output phase and until the user supplies the authoritative source/timing data.

ESP32 warning:
GPIO34, GPIO35, GPIO36, and GPIO39 are input-only and do not provide internal pull-up/pull-down resistors.

======================================================================
GLOBAL SOFTWARE RULES
======================================================================

The firmware must be:
- modular,
- non-blocking,
- testable,
- recoverable,
- configurable,
- fault tolerant,
- storage aware,
- network independent,
- future ESP32-S3 ready.

Do not:
- create one monolithic .ino file,
- use long blocking delay() calls,
- place network calls in timing-critical input code,
- write counters to flash every second,
- hard-code vehicle-specific calibration in core logic,
- hard-code future emergency ON/OFF timings before source data is supplied,
- silently remove previously working functions,
- change frozen interfaces from earlier phases without explicit approval.

Prefer:
- explicit interfaces,
- clear ownership of each module,
- state machines,
- hardware timers / esp_timer where appropriate,
- PCNT or suitable ESP32 peripherals for pulse counting,
- versioned configuration,
- persistent queues,
- structured logging,
- compile-time feature flags for incomplete future hardware.

======================================================================
PHASE-INDEPENDENCE RULE
======================================================================

This project is intentionally divided into independent coding phases.

The purpose is to make each phase:
- buildable,
- testable,
- reviewable,
- stable before the next phase begins.

Once a phase is accepted, its public interfaces and behavior become FROZEN.

Later phases must extend the system through predefined interfaces instead of rewriting earlier modules.

If a later phase discovers that an earlier frozen interface is insufficient:

1. STOP.
2. Explain the exact limitation.
3. Propose a backward-compatible extension or a versioned interface.
4. Do NOT silently refactor the old phase.
5. Wait for user approval before changing the frozen contract.

Minimal integration wiring is allowed, but completed core logic from earlier phases must not be redesigned without approval.

======================================================================
REQUIRED CLAUDE CODE WORKFLOW
======================================================================

Before coding this phase:

1. Inspect the repository.
2. Read README, configuration files, existing source, tests, and previous phase result notes.
3. Verify the prerequisite phase is complete.
4. List existing frozen interfaces that this phase will use.
5. List files that will be added.
6. List any existing files that require minimal integration changes.
7. List all TBD values.
8. Present a short implementation plan.
9. Then implement ONLY this phase.

After coding this phase:

1. Build/compile the project.
2. Fix compile errors.
3. Run or clearly define all acceptance tests for this phase.
4. Do not claim PASS for a test that was not actually performed.
5. Update README / phase notes / CHANGELOG as appropriate.
6. Produce the required Phase Result Report.
7. STOP. Do not start the next phase.

The user will return the Phase Result Report for external review before authorizing the next phase.


======================================================================
PREREQUISITE
======================================================================

Phases 1 and 2 must be accepted.

Use Phase 2 input interfaces exactly as designed.

Do not rewrite input drivers to implement business logic.

======================================================================
PHASE 3 OBJECTIVE
======================================================================

Implement the local vehicle state engine, runtime counters, distance calculation, structured events, and persistent counter recovery.

The complete local operating logic must work with networking fully disabled.

======================================================================
SCOPE
======================================================================

A. Vehicle State Manager

Treat IGNITION and ENGINE_RUNNING as separate states.

For current bench testing:
ENGINE_RUN digital input is the primary engine-running source.

Design the state manager so future sources can be substituted:
- RPM,
- alternator D+,
- oil pressure,
- dedicated engine-run input.

Do not rewrite the counter manager when the source changes later.

B. Engine Runtime Counter

Accumulate only while validated ENGINE_RUNNING = true.

Maintain:
- high-resolution internal runtime,
- lifetime engine hours,
- optional daily runtime accumulator.

Do not write flash every second.

C. PTO Runtime Counter

Configurable rule:
- PTO only,
or
- PTO AND ENGINE_RUNNING.

Do not hard-code the rule.

D. Speed and Distance

From VEHICLE_SPEED pulse interface:

Use configurable:
speed_pulse_per_km = TBD until calibrated per vehicle.

Calculate:
- current speed,
- trip distance,
- daily distance,
- lifetime calculated distance / odometer.

Do not fabricate calibration.

E. RPM

Use configurable:
rpm_pulse_per_rev = TBD.

Calculate RPM only when calibrated.

F. Structured Events

Generate through Phase 1 event interface:

IGN_ON
IGN_OFF
ENGINE_START
ENGINE_STOP
PTO_ON
PTO_OFF

Event generation must be edge-driven and not duplicated every loop.

G. Persistent Counter Storage

Persist:
- engine runtime,
- PTO runtime,
- lifetime distance,
- event sequence number if appropriate,
- required recovery metadata.

Use flash-wear-aware checkpoint strategy.

H. Recovery

After reboot:
- restore counters,
- continue from saved state,
- never reset lifetime counters to zero,
- never decrease valid counters because of restart.

I. Counter Snapshot

Populate the Phase 1 CounterSnapshot contract.

J. TFT

Add pages for:
- engine state,
- PTO state,
- engine hours,
- PTO hours,
- speed,
- distance,
- RPM.

K. Emergency Output

Remain disabled and untouched.

======================================================================
OUT OF SCOPE
======================================================================

Do not implement:
- network transport,
- telemetry upload,
- server business logic,
- PM calculations,
- part lifetime calculations,
- remote configuration,
- remote commands,
- GPS,
- OTA,
- real emergency output.

======================================================================
PHASE 3 ACCEPTANCE TESTS
======================================================================

1. IGN ON alone does not automatically increment engine hours.
2. ENGINE_RUN start generates one ENGINE_START event.
3. ENGINE_RUN stop generates one ENGINE_STOP event.
4. Engine runtime increases only while engine-running state is true.
5. PTO rule works in both configurable modes.
6. Speed pulse count converts to distance using test calibration.
7. RPM pulse converts using test calibration.
8. Reboot restores counters.
9. Repeated reboot never causes counters to decrease.
10. Counter persistence does not write flash every second.
11. Input drivers from Phase 2 remain unchanged except minimal integration if already planned.
12. Networking can be completely absent and all tests still pass.
13. Emergency physical output remains disabled.

======================================================================
PHASE 3 EXIT GATE
======================================================================

This is the frozen local measurement core.

After acceptance, later phases must consume CounterSnapshot and DeviceEvent outputs rather than modifying the counter algorithms.

======================================================================
REQUIRED PHASE RESULT REPORT
======================================================================

At the end of this phase, output exactly this review structure:

PHASE:
STATUS: PASS / PARTIAL / BLOCKED

OBJECTIVE:

PREREQUISITE CHECK:

FROZEN INTERFACES USED:

FILES ADDED:

FILES MODIFIED:

IMPLEMENTATION SUMMARY:

BUILD RESULT:

TESTS ACTUALLY PERFORMED:

TEST RESULTS:

SERIAL MONITOR SAMPLE:

TFT RESULT, IF APPLICABLE:

TBD VALUES REMAINING:

KNOWN LIMITATIONS:

RISKS / CONCERNS:

ANY CHANGE TO PREVIOUS FROZEN PHASES:
- Must normally be NONE.
- If not NONE, explain every change and why it was unavoidable.

NEXT PHASE READINESS:
READY / NOT READY

STOP HERE.
DO NOT IMPLEMENT THE NEXT PHASE.
