CLAUDE CODE IMPLEMENTATION PROMPT
CRANE FLEET MAINTENANCE / IOT WEB APPLICATION AND BACKEND API
PHASE 9 - OTA, SAFETY-CRITICAL COMMAND FRAMEWORK, AND EMERGENCY OUTPUT READINESS
Revision 01

======================================================================
PROJECT CONTEXT
======================================================================

Act as a senior full-stack software architect, backend/API engineer, database architect, UX engineer, and industrial fleet-maintenance systems engineer.

This project is a centralized system for managing:
- mobile cranes,
- related vehicles,
- workshop maintenance equipment,
- vehicle/model master data,
- QR-based machine access,
- daily/weekly inspections,
- preventive maintenance,
- repair orders,
- spare parts,
- part sets / kits,
- part lifetime,
- transferable major components,
- drivers/operators,
- certificates,
- technical documents,
- work history,
- engine/PTO/distance counters,
- GPS,
- alerts,
- IoT devices,
- telemetry,
- online configuration,
- remote commands,
- OTA,
- future safety-related emergency output,
- roles/permissions,
- audit history.

All user-facing Web pages must be in THAI.

Backend/API/database identifiers and stable status codes should use ENGLISH technical names.

======================================================================
TARGET ARCHITECTURE
======================================================================

Prototype:

Thai Web Application
    ->
Backend API
    ->
Domain / Service Layer
    ->
Repository Interface
    ->
Google Sheets Repository

Production:

Thai Web Application
    ->
Backend API
    ->
Domain / Service Layer
    ->
Repository Interface
    ->
PostgreSQL Repository

IMPORTANT:

1. The browser must never read/write Google Sheets directly.
2. The Web must call the Backend API.
3. ESP32 devices must call the Backend/Device API, not Google Sheets.
4. Business logic belongs in backend/domain services, not spreadsheet formulas.
5. The repository layer isolates Google Sheets from business logic.
6. Replacing Google Sheets with PostgreSQL later must not require rewriting Web workflows.
7. API contracts must be versioned and stable.
8. Historical records must not be overwritten.
9. Revision-controlled masters must preserve old historical meaning.
10. Do not fabricate missing real-world data.

======================================================================
LANGUAGE / UX RULE
======================================================================

ALL USER-FACING WEB CONTENT MUST BE IN THAI, including:
- navigation,
- page titles,
- buttons,
- labels,
- form instructions,
- validation messages,
- errors,
- warnings,
- status labels,
- confirmations,
- tables,
- filters,
- inspection pages,
- PM pages,
- repair pages,
- device management,
- command status,
- safety warnings.

Internal stable codes remain English.

======================================================================
QR RULE
======================================================================

One crane must have ONE permanent QR code.

QR target:

/vehicle/{vehicle_id}

The QR opens Vehicle Detail, not a dedicated inspection-only page.

Workshop equipment may use:

/equipment/{equipment_id}

and one permanent QR per machine.

======================================================================
PHASE-INDEPENDENCE RULE
======================================================================

Before coding:
1. Inspect the repository.
2. Read previous phase reports.
3. Verify prerequisites.
4. List frozen contracts from earlier phases.
5. List files to add.
6. List any existing files requiring minimal integration changes.
7. List TBD items.
8. Present the implementation plan.
9. Implement ONLY this phase.

After coding:
1. Run build/type-check/lint as applicable.
2. Run tests for this phase.
3. Do not claim PASS if a test was not actually performed.
4. Update API docs / README / CHANGELOG.
5. Produce the Phase Result Report.
6. STOP.
7. Do NOT start the next phase.

If an earlier frozen interface must change:
STOP before changing it and explain why.

======================================================================
REQUIRED PHASE RESULT REPORT
======================================================================

PHASE:
STATUS: PASS / PARTIAL / BLOCKED

OBJECTIVE:

PREREQUISITE CHECK:

FROZEN CONTRACTS USED:

FILES ADDED:

FILES MODIFIED:

API ROUTES ADDED:

DATABASE / SHEET TABLES USED:

UI PAGES ADDED:

IMPLEMENTATION SUMMARY:

BUILD / TYPECHECK / LINT RESULT:

TESTS ACTUALLY PERFORMED:

TEST RESULTS:

SCREEN / UX NOTES:

TBD VALUES REMAINING:

KNOWN LIMITATIONS:

RISKS / CONCERNS:

ANY CHANGE TO PREVIOUS FROZEN PHASES:
- Normally NONE.

NEXT PHASE READINESS:
READY / NOT READY

STOP HERE.
DO NOT IMPLEMENT THE NEXT PHASE.


PREREQUISITE

Phases 1-8 accepted.

Actual fixed ON/OFF emergency timing belongs to approved ESP32 firmware/profile engineering data supplied later by the user. Do not invent waveform timing here.

OBJECTIVE

Implement OTA management and backend/Web safety framework for the future emergency-related output.

SCOPE

A. Firmware release metadata:
version
hardware compatibility
channel
checksum
signature metadata
storage_ref
status.

B. Deployment:
DEV
PILOT
GROUP
FLEET
with target/status/result/failure/timestamps.

C. Firmware binary lives in file/object storage, not Sheet/PostgreSQL cells.

D. Separate safety-critical command domain from normal remote commands.

Future command:
REMOTE_INHIBIT or approved equivalent.

E. Safety command UI/backend must support:
exact vehicle/device target
model
online state
engine/PTO state if available
last seen
GPS if available
reason
short TTL
explicit confirmation
MFA/step-up integration point
command ID
ACK
execution state
physical feedback state
audit trail.

F. Stale command protection.

G. Emergency profile metadata:
approved profile ID
model ID
revision
compatibility
status.

Do not allow ordinary Web editing of individual fixed timing values.

H. Physical feedback states:
NOT_AVAILABLE
PENDING
CONFIRMED
FAILED.

I. Local reset/recovery status fields.

J. Thai safety UI must distinguish:
sent
device acknowledged
executing
output activated
physical feedback confirmed
failed
expired.

K. Do not label as certified E-Stop unless the complete engineered system qualifies.

ACCEPTANCE TESTS

OTA:
1. release metadata works.
2. staged deployment works.
3. incompatible device can be rejected.
4. binary is outside database.

Safety:
5. separate safety workflow exists.
6. TTL is mandatory.
7. stale command cannot remain executable.
8. exact target/model is shown before confirmation.
9. reason is required.
10. states are distinct.
11. physical feedback is separate from output execution.
12. timing values are not freely editable.
13. audit context exists.
14. feature is not described as certified E-Stop.

EXIT GATE

Backend/Web is ready for approved ESP32 emergency profiles without redesign.
